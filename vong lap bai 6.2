#include<stdio.h>

int main(){
	int n;
	scanf("%d",&n);
	int sum=0;
	for(int i=1;i<=n;i++){
	  if (n%i==0)
		sum+=i;
	}
	if (sum==n*2)
	    printf("%d la so hoan hao",n);
	else printf("%d ko la so hoan hao",n);
  return 0;
}
