#include<stdio.h>

int main(){
	int n,sl=0,x;
	scanf("%d",&n);
	int a[n];
	printf("nhap mang :");
	for (int i=0;i<n;i++)  scanf("%d",&a[i]);
	printf("x=");scanf("%d",&x);
	for(int i=0;i<n;i++){
		if (a[i]==x) sl++;
	}
	printf("so lan x xuat hien trong mang la :%d,",sl);
	return 0;
}
