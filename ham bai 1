#include<stdio.h>
float luyth(float x,int y){
	float k=1;
	for(int i=0;i<y;i++)
	  k*=x;
	return k;
}
int main() {
	float x; int y;
	printf("x,y="); scanf ("%f %d",&x,&y);
	printf("x^y=%.2f",luyth(x,y));
	return 0;
}
